spring:
  datasource:
    hikari:
      driver-class-name: org.h2.Driver
#      jdbc-url: jdbc:h2:./data/mealimeter # ./ Project root
      jdbc-url: jdbc:h2:tcp://localhost:9093/./data/mealimeter
      username: sa
      password:
  h2:
    console:
      enabled: true
      path: /h2 # default /h2-console
  jpa:
    hibernate:
      ddl-auto: update
